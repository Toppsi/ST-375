clear

%% Loader data
A = load('Baseline_30sek_0g')

%% Gennemsnitsværdi
Offset = mean(A.data)

%% FFT
Fs = 500;
T = 1/Fs;
N = length(A.data)

xdft = fft(A.data-Offset)/N;

%A1 = fft(A.data, NFFT)/L;

%meanfreq(A.data)



A1 = fft(A.data)
